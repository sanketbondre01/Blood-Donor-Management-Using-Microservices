package com.example.sanket.repo;

import org.springframework.data.jpa.repository.JpaRepository;

import com.example.sanket.entity.RequestBlood;

public interface RequestBloodRepository extends JpaRepository<RequestBlood, Long> {
}
